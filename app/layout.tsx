import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'AngularJS Learning Hub - Master JavaScript & ES6',
  description: 'Learn AngularJS from scratch with interactive examples, JavaScript basics, and ES6 features. Fun, practical, hands-on tutorials.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
